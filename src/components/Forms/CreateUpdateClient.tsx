import React from "react"
import { Screen, SectionHeader } from "../"

const CreateUpdateClient = () => {
  return (
    <Screen>
      <SectionHeader title='Add a Client' center />
      
    </Screen>
  )
}

export default CreateUpdateClient
