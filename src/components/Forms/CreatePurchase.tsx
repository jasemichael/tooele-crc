import React from "react"
import { Screen } from "../"
import SectionHeader from "../SectionHeader"

const CreatePurchase = () => {
  return (
    <Screen>
      <SectionHeader title='Make a Purchase' center />
    </Screen>
  )
}

export default CreatePurchase
