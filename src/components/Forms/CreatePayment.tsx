import React from "react"
import { Screen } from "../"
import SectionHeader from "../SectionHeader"

const CreatePayment = () => {
  return (
    <Screen>
      <SectionHeader title='Pay Client' center />
    </Screen>
  )
}

export default CreatePayment
