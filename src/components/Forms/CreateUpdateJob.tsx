import React from "react"
import { Screen, SectionHeader } from "../"

const CreateUpdateJob = () => {
  return (
    <Screen>
      <SectionHeader title='Add a Job' center />
    </Screen>
  )
}

export default CreateUpdateJob
