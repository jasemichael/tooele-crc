import React from "react"
import { Screen, SectionHeader } from "../"

const CreateUpdateEmployee = () => {
  return (
    <Screen>
      <SectionHeader title='Add an Employee' center />
    </Screen>
  )
}

export default CreateUpdateEmployee
