import React from "react"
import { Screen } from "../"
import SectionHeader from "../SectionHeader"

const CreateUpdateNote = () => {
  return (
    <Screen>
      <SectionHeader title='Add Note' center />
    </Screen>
  )
}

export default CreateUpdateNote
