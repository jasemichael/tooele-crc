import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    title: {
        fontSize: 32,
        textAlign: "center",
        fontWeight: "bold"
    }
})

export default globalStyles
