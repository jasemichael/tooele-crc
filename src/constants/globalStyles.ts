import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    title: {
        fontSize: 32,
        textAlign: "center",
        fontWeight: "bold"
    },
    textInput: {
        borderRadius: 5,
        borderColor: "black",
        borderStyle: "solid",
        borderWidth: 1,
        padding: 8,
        width: "100%",
        paddingLeft: 40
    }
})

export default globalStyles
